import 'package:flutter/material.dart';

/// Nebah Design System Color Palette
/// 
/// Modern Security-Tech aesthetic with full Light and Dark mode support.
class NebahColors {
  // Primary - Trust & Authority
  static const Color darkPrimary = Color(0xFF1A56DB);      // Cobalt Blue
  static const Color lightPrimary = Color(0xFF1E3A8A);     // Deep Navy

  // Secondary / Accent - Safety & Verification
  static const Color darkSecondary = Color(0xFF057A55);    // Safety Emerald
  static const Color lightSecondary = Color(0xFF047857);   // Emerald Green

  // Emergency / SOS
  static const Color darkEmergency = Color(0xFFE02424);    // Crimson Red
  static const Color lightEmergency = Color(0xFFDC2626);   // Vibrant Red

  // Warning & Alerts
  static const Color warning = Color(0xFFD97706);          // Amber
  static const Color warningLight = Color(0xFFFFFBEB);

  // Background & Surfaces - Dark Mode
  static const Color darkBackground = Color(0xFF0F172A);   // Navy Slate Base
  static const Color darkSurface = Color(0xFF1E293B);      // Dark Slate Card
  static const Color darkSurfaceElevated = Color(0xFF334155);

  // Background & Surfaces - Light Mode
  static const Color lightBackground = Color(0xFFF8FAFC);  // Soft Off-White
  static const Color lightSurface = Color(0xFFFFFFFF);     // Pure White
  static const Color lightSurfaceElevated = Color(0xFFF1F5F9);

  // Text Colors - Dark Mode
  static const Color darkTextPrimary = Color(0xFFF8FAFC);
  static const Color darkTextSecondary = Color(0xFF94A3B8);
  static const Color darkTextMuted = Color(0xFF64748B);

  // Text Colors - Light Mode
  static const Color lightTextPrimary = Color(0xFF0F172A);
  static const Color lightTextSecondary = Color(0xFF475569);
  static const Color lightTextMuted = Color(0xFF94A3B8);

  // Borders & Dividers
  static const Color darkBorder = Color(0xFF334155);
  static const Color lightBorder = Color(0xFFE2E8F0);

  // Convenience Aliases for modern Security-Tech UI
  static const Color cobaltBlue = Color(0xFF1A56DB);
  static const Color deepNavy = Color(0xFF1E3A8A);
  static const Color safetyEmerald = Color(0xFF057A55);
  static const Color crimsonRed = Color(0xFFE02424);
  static const Color navyBackground = Color(0xFF0F172A);
  static const Color slate900 = Color(0xFF0F172A);
  static const Color slate800 = Color(0xFF1E293B);
  static const Color slateGrey = Color(0xFF94A3B8);
}
